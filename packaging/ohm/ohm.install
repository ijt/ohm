post_install() {
  echo "Enable the warm daemon:"
  echo "  systemctl --user enable --now ohm.service"
  echo "Then set as default (Omarchy Setup > Defaults > Browser once listed), or:"
  echo "  xdg-settings set default-web-browser ohm.desktop"
}

post_upgrade() {
  post_install
}
